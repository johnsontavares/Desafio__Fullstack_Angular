export interface Student {
  codAluno: string; // Assuming an ID field for students
  nome: string;
  dNascimento: string;
  cpf: string;
  endereco: string;
  celular: string;
  iCodeEscola: string
  // Add other relevant student data fields here (e.g., course, grade)
}
